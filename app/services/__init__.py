"""AI 提示词目录。

每个 AI 能力对应一个独立的 ``.md`` 文件，把提示词从代码里抽出来单独维护：

* 调优提示词不需要改 Python 代码，也不需要重新跑测试；
* 提示词可以被 review、被 diff，纳入版本管理；
* 不同能力之间不会互相污染（生成用例和失败分析对模型的角色要求完全不同）。

命名约定：``<能力名>.md``，与 ``ai_service.py`` 里的方法一一对应。
"""
